import 'package:flutter/material.dart';

class VendeurReportsScreen extends StatelessWidget {
  const VendeurReportsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rapports'),
      ),
      body: const Center(
        child: Text('Écran Rapports - À implémenter'),
      ),
    );
  }
}

