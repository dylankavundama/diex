import 'package:flutter/material.dart';

class VendeurCashScreen extends StatelessWidget {
  const VendeurCashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Caisse'),
      ),
      body: const Center(
        child: Text('Écran Caisse - À implémenter'),
      ),
    );
  }
}

